@model IEnumerable<BITCollege_Felipe_Rincon.Models.StudentCard>

@{
    ViewBag.Title = "Index";
    /// StudentCards Index view modified
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CardNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Student)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>   
            @Html.DisplayFor(modelItem => item.CardNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Student.FullName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.StudentCardId }) |
            @Html.ActionLink("Details", "Details", new { id=item.StudentCardId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.StudentCardId })
        </td>
    </tr>
}

</table>
